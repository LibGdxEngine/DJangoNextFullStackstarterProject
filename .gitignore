# Local environment configuration
.env
.env.*
!.env.example
!.env.prod.example

# Private OCR source files must never enter git or Docker build contexts.
backend/private/

# Local databases may contain encrypted provider credentials and account metadata.
backend/*.sqlite3
backend/*.sqlite3-*
